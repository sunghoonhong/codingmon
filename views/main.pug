extends layout

block content
  if !user
    h1 Hello, Codingmon!
    h1 로그인을 해주세요
  else if user.type == 'admin'
    h2 관리자 모드

  else if user.type == 'freelancer'
    h2 프리랜서 모드

  else if user.type == 'client'
    h2 의뢰자 모드